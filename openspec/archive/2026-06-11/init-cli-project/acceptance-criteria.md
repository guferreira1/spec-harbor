# Acceptance Criteria: Initialize SpecHarbor CLI Project

- The repository contains a Go CLI entrypoint at `cmd/specharbor/main.go`.
- The command `go test ./...` succeeds.
- The CLI can print help output.
- The CLI can print a development version.
- OpenSpec project context exists at `openspec/project.md`.
- The first OpenSpec change exists under `openspec/changes/init-cli-project`.
- CI is configured through GitHub Actions.
