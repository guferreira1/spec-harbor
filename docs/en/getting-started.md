# Getting Started

Use the current command reference instead of this legacy quickstart:

- [Usage](usage.md)
- [Workflow](workflow.md)

The implemented prompt syntax uses `--role` and optional `--agent`, which defaults to `generic` and only adapts generated prompt text.
