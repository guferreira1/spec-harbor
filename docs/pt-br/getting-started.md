# Primeiro passos

Use a referência de comando atual no lugar deste início rápido legado:

- [Uso](usage.md)
- [Fluxo de trabalho](workflow.md)

A sintaxe de prompt implementada usa `--role` e `--agent` opcional, que usa `generic` por padrão e apenas adapta o texto do prompt gerado.
